sudo apt-get update
sudo apt-get install docker-ce-cli containered.io
sudo docker run hello-world
sudo chmod 666 /var/run/docker.sock
docker pull mongo 
docker run -d -p 27017:27017 --name mongocontainer mongo:latest
docker image 
mongo ps


//To Build -- go to project folder and type the below command

(Note: There is (dot) at the end)

---> docker build -t spring-containerimage .

//To Run the project in docker container type the below command:

---> docker run --name spring-app --network=host spring-containerimage:latest

//to run docker-compare.yml
//to run the docker-compse file build all the images and run the container

---> docker-compose up --build

//stop all the container and remove all the container

---> docker-compose down 
